# Python
__pycache__/
**/__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Environment variables & secrets
.env
.env.local
.env.production
secrets/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data — hide entire data/ except pre-seeded runs.db (kept for Render/history)
data/*
!data/runs.db
eval/**/runtime/*.db

# Runtime-tunable detection thresholds: mutated by POST /api/v1/analysis/thresholds,
# so a tracked copy pins whatever a developer last tuned it to and silently
# overrides the code-level defaults in diagnostics_config.py for everyone who
# pulls it — including test isolation that monkeypatches those defaults.
data/diagnostics/thresholds.json


# Logs
# Anchored to the repo root: an unanchored `logs/` also swallowed the
# `app/api/runs/[runId]/logs/` route, and git cannot re-include a file whose
# parent directory is excluded. Build-cache logs live under .next/, ignored separately.
/logs/
*.log
frontend/*.log
.e2e-*.log

# Test coverage
htmlcov/
.coverage
coverage.xml

# Docker
docker-compose.override.yml

# Node (frontend)
node_modules/
.next/
out/
.nuxt/

# Build
*.pyc
*.pyo

# AI logs — tracked structure, ignored content
.ai-log/
.ai-log/*.jsonl
.ai-log/archive/

.codegraph/
.kiro/
.kilo/
.next-builds/
.npmrc
frontend/.npmrc

.pytest_cache/
.ruff_cache/

# Terraform local working directories
# Terraform & Infrastructure
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.hcl
.terraform.lock.hcl
terraform/gcp/.terraform/

nginx/certs/
.opencode/plans/
.superpowers/
