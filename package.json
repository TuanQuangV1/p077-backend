{
    "scripts": {
        "sync": "git checkout develop && git pull origin develop && git checkout deploy/render && git merge develop --no-edit && git push personal deploy/render:main && git checkout develop",
        "frontend:dev": "pnpm --dir frontend dev",
        "frontend:lint": "pnpm --dir frontend lint",
        "frontend:build": "pnpm --dir frontend build"
    },
    "devDependencies": {
        "@types/node": "^26.1.1",
        "typescript": "^7.0.2"
    },
    "onlyBuiltDependencies": [
        "esbuild",
        "sharp"
    ]
}